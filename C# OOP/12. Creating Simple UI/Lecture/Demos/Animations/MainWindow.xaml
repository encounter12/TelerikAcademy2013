<Window x:Class="Animations.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="MainWindow" Height="350" Width="525">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>
        <Canvas Name="BallsCanvas"
                Width="500"
                Height="300"
                Grid.Row="0">
        </Canvas>
        <UniformGrid Grid.Row="1"
                     Columns="2">
            <Button Name="ButtonStart"
                    Click="OnStartButtonClick">Start</Button>
            <Button Name="ButtonStop"
                    Click="OnStopButtonClick"
                    IsEnabled="False">Stop</Button>
        </UniformGrid>
    </Grid>
</Window>
